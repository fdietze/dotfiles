result
target/

# noctalia v5: only home/noctalia/settings.toml (GUI state) and templates/ are
# tracked. Runtime artifacts (generated colors, clipboard/plugin caches) live
# under ~/.config/noctalia and ~/.local/state/noctalia, never in the repo.
