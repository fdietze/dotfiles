#!/usr/bin/env bash

# if [ $(cat $HOME/.theme) = "dark" ]; then
#     style='-fn "Roboto Mono-8" -nb "#121212" -nf "#EFEFEF" -sb "#37BAFF" -sf "#101010"'
# else
#     style='-fn "Roboto Mono-8" -nb "#FFFFFF" -nf "#000000" -sb "#00D7FF" -sf "#101010"'
# fi

# if command -v yeganesh 2>/dev/null; then
#     command=$(bash -c "yeganesh -x -- -i $style")
# else 
#     command=$(bash -c "dmenu_path | dmenu -i $style")
# fi

# launch interactive zsh, to expand aliases
# exec zsh -i -c "$command"

# exec rofi -theme Arc -show run

# if [ $(cat $HOME/.theme) = "dark" ]; then
#   ROFI_ICON_THEME="Qogir-dark"
# else
#   ROFI_ICON_THEME="Qogir"
# fi

# exec rofi \
#   -font "Monospace 32" \
#   -theme "$(cat $HOME/.theme)" \
#   -icon-theme "$ROFI_ICON_THEME" \
#   -show-icons \
#   -show run \
#   -run-command "zsh -ic \"{cmd}\""
#   -run-shell-command 'alacritty -e zsh -ic "{cmd} && read"'

