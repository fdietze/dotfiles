# Temporäre Dev-Links (`my.devLinks`) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Ein allgemeiner Home-Manager-Schalter `my.devLinks`, der gelistete repo-relative Pfade temporär out-of-store ans Working Tree linkt (schnelles Feedback), während der Default voll reproduzierbar (Store) bleibt.

**Architecture:** Ein neues HM-Modul `dev-links.nix` deklariert die Option `my.devLinks` und stellt über `_module.args` die Helper-Funktion `mkDevSource relPath storePath` bereit. `dotfiles.nix` (via `collectFiles`) und `pi-extensions.nix` nutzen sie, um pro Datei zwischen Store und `mkOutOfStoreSymlink` zu wählen. `agent.json` wird nix-generiert und hängt die aus `my.devLinks` abgeleiteten Sandbox-Leserechte automatisch an.

**Tech Stack:** Nix, Home-Manager (`config.lib.file.mkOutOfStoreSymlink`, `pkgs.formats.json`), nono sandbox profiles.

**Verifikations-Hinweis:** Reine Nix-Modul-Änderung — kein Unit-Test-Framework. "Tests" = `nixos-rebuild build` (keine Aktivierung, erlaubt) + gezielte `nix eval`-Assertions. System-aktivierende Switches (`nrs`) macht **immer der User selbst**; entsprechende Schritte sind als manuell markiert.

---

## File Structure

- Create: `modules/home-manager/dev-links.nix` — Option `my.devLinks` + `_module.args.mkDevSource`.
- Modify: `modules/home-manager/profiles/shell-core.nix` — Import des neuen Moduls.
- Modify: `modules/home-manager/profiles/ai-agents/pi-extensions.nix` — Quelle pro Extension über `mkDevSource`.
- Modify: `modules/home-manager/dotfiles.nix` — `collectFiles` über `mkDevSource`; `opencode.jsonc` Store-Default; `agent.json` nix-generiert mit abgeleiteten Read-Pfaden.

---

### Task 1: Modul `dev-links.nix` (Option + Helper)

**Files:**
- Create: `modules/home-manager/dev-links.nix`
- Modify: `modules/home-manager/profiles/shell-core.nix` (imports-Liste)

- [ ] **Step 1: Modul schreiben**

Create `modules/home-manager/dev-links.nix`:

```nix
# Temporärer Dev-Link-Schalter: gelistete repo-relative Pfade werden out-of-store
# ans Working Tree gelinkt (schnelles Feedback ohne Rebuild pro Edit), statt aus
# dem Store gepinnt. Default leer = voll reproduzierbar. Siehe
# docs/superpowers/specs/2026-06-16-dev-links-design.md.
#
# `mkDevSource relPath storePath` liefert den `source`-Wert für home.file/
# xdg.configFile: gelistet -> mkOutOfStoreSymlink, sonst die gepinnte Store-Quelle.
{
  config,
  lib,
  ...
}: let
  repoDir = "${config.home.homeDirectory}/projects/dotfiles";
in {
  options.my.devLinks = lib.mkOption {
    type = lib.types.listOf lib.types.str;
    default = [];
    example = ["modules/home-manager/profiles/ai-agents/pi-extensions/context-prune.ts"];
    description = ''
      Repo-relative Pfade (ab Repo-Wurzel), die temporär out-of-store ans
      Working Tree gelinkt werden statt aus dem Store. Für schnelles Feedback
      während der Entwicklung; nach Stabilisierung wieder entfernen.
    '';
  };

  config._module.args.mkDevSource = relPath: storePath:
    if builtins.elem relPath config.my.devLinks
    then config.lib.file.mkOutOfStoreSymlink "${repoDir}/${relPath}"
    else storePath;
}
```

- [ ] **Step 2: Modul importieren**

In `modules/home-manager/profiles/shell-core.nix`, imports-Liste, direkt nach `../dotfiles.nix`:

```nix
    ../dotfiles.nix
    ../dev-links.nix
```

- [ ] **Step 3: Build verifizieren (Eval ok, Option existiert)**

Run: `nixos-rebuild build --flake .#gurke 2>&1 | tail -5`
Expected: Build erfolgreich, kein Eval-Fehler. (Ohne Konsumenten ist `mkDevSource` ungenutzt — das ist ok.)

- [ ] **Step 4: Commit**

```bash
git add modules/home-manager/dev-links.nix modules/home-manager/profiles/shell-core.nix
git commit -m "feat(home): add my.devLinks option and mkDevSource helper"
```

---

### Task 2: `pi-extensions.nix` über `mkDevSource`

**Files:**
- Modify: `modules/home-manager/profiles/ai-agents/pi-extensions.nix`

- [ ] **Step 1: Modul umstellen**

Ersetze den gesamten Inhalt von `modules/home-manager/profiles/ai-agents/pi-extensions.nix` durch:

```nix
# pi-spezifische Extensions in den Auto-Discovery-Pfad ~/.pi/agent/extensions/
# verlinken (siehe pi-Doku: extensions.md → "Extension Locations"). Top-Level-*.ts
# werden als einzelne Extensions verlinkt; Unterverzeichnisse mit index.ts als
# mehrdateiige Extensions (pi lädt nur deren index.ts; engine.ts/feed.ts/*.test.ts
# bleiben inert). Bearbeitung im Repo + Home-Manager-Switch; danach `/reload` in pi.
#
# Quelle pro Extension via mkDevSource: per my.devLinks gelistete Pfade werden
# out-of-store ans Working Tree gelinkt (schnelles Feedback), sonst Store.
{
  lib,
  mkDevSource,
  ...
}: let
  dir = ./pi-extensions;
  # Repo-relativer Präfix dieses Verzeichnisses (Matching-Key für my.devLinks).
  relRoot = "modules/home-manager/profiles/ai-agents/pi-extensions";
  entries = builtins.readDir dir;

  mkEntry = name: {
    name = ".pi/agent/extensions/${name}";
    value.source = mkDevSource "${relRoot}/${name}" (dir + "/${name}");
  };

  # Top-Level *.ts -> ~/.pi/agent/extensions/<name>
  files =
    lib.mapAttrs' (name: _: mkEntry name)
    (lib.filterAttrs (name: type: type == "regular" && lib.hasSuffix ".ts" name) entries);

  # Unterverzeichnisse mit index.ts -> ~/.pi/agent/extensions/<dir>
  subdirs =
    lib.mapAttrs' (name: _: mkEntry name)
    (lib.filterAttrs
      (name: type: type == "directory" && builtins.pathExists (dir + "/${name}/index.ts"))
      entries);
in {
  home.file = files // subdirs;
}
```

- [ ] **Step 2: Build verifizieren (Default = Store)**

Run: `nixos-rebuild build --flake .#gurke 2>&1 | tail -5`
Expected: Build erfolgreich.

- [ ] **Step 3: Default-Quelle prüfen (zeigt in den Store, nicht out-of-store)**

Run:
```bash
nix eval --impure --raw --expr '
  let hm = (builtins.getFlake (toString ./.)).nixosConfigurations.gurke.config.home-manager.users.felix;
  in builtins.toString hm.home.file.".pi/agent/extensions/context-prune.ts".source'
```
Expected: ein `/nix/store/...`-Pfad (kein `/home/felix/projects/dotfiles/...`).

- [ ] **Step 4: Dev-Link-Verhalten prüfen (mit gesetztem devLinks)**

Run:
```bash
nix eval --impure --raw --expr '
  let
    flake = builtins.getFlake (toString ./.);
    sys = flake.nixosConfigurations.gurke.extendModules {
      modules = [{ home-manager.users.felix.my.devLinks =
        [ "modules/home-manager/profiles/ai-agents/pi-extensions/context-prune.ts" ]; }];
    };
  in builtins.toString sys.config.home-manager.users.felix.home.file.".pi/agent/extensions/context-prune.ts".source'
```
Expected: `/home/felix/projects/dotfiles/modules/home-manager/profiles/ai-agents/pi-extensions/context-prune.ts` (out-of-store, zeigt ins Working Tree).

- [ ] **Step 5: Commit**

```bash
git add modules/home-manager/profiles/ai-agents/pi-extensions.nix
git commit -m "feat(pi-extensions): source extensions via mkDevSource"
```

---

### Task 3: `collectFiles` über `mkDevSource` + `opencode.jsonc` Store-Default

**Files:**
- Modify: `modules/home-manager/dotfiles.nix`

**Hinweis:** `collectFiles` braucht den repo-relativen Wurzel-Präfix, um den
Matching-Key für `my.devLinks` zu bilden. Aktuell sind die Call-Sites
`collectFiles ../../home/config [...]` (→ `xdg.configFile`) und
`collectFiles ../../home/files [...]` (→ `home.file`). Der Präfix ist
`home/config` bzw. `home/files`.

- [ ] **Step 1: `collectFiles`-Signatur um den repo-relativen Präfix erweitern**

In `modules/home-manager/dotfiles.nix`, ersetze die `collectFiles`-Definition:

```nix
  collectFiles = root: skipped: let
    walk = prefix: dir:
      mergeAttrs (
        lib.mapAttrsToList (
          name: type: let
            relPath =
              if prefix == ""
              then name
              else "${prefix}/${name}";
            path = dir + "/${name}";
          in
            if builtins.elem relPath skipped
            then {}
            else if type == "directory"
            then walk relPath path
            else if type == "regular" || type == "symlink"
            then {
              "${relPath}" = {
                source = path;
              };
            }
            else {}
        ) (builtins.readDir dir)
      );
  in
    walk "" root;
```

durch (neuer erster Parameter `repoRelRoot`, Quelle via `mkDevSource`):

```nix
  collectFiles = repoRelRoot: root: skipped: let
    walk = prefix: dir:
      mergeAttrs (
        lib.mapAttrsToList (
          name: type: let
            relPath =
              if prefix == ""
              then name
              else "${prefix}/${name}";
            path = dir + "/${name}";
          in
            if builtins.elem relPath skipped
            then {}
            else if type == "directory"
            then walk relPath path
            else if type == "regular" || type == "symlink"
            then {
              "${relPath}" = {
                source = mkDevSource "${repoRelRoot}/${relPath}" path;
              };
            }
            else {}
        ) (builtins.readDir dir)
      );
  in
    walk "" root;
```

- [ ] **Step 2: Modul-Argumente erweitern**

In `modules/home-manager/dotfiles.nix` den Header

```nix
{
  config,
  lib,
  ...
}: let
  repoDir = "${config.home.homeDirectory}/projects/dotfiles";
```

ersetzen durch (fügt `mkDevSource` hinzu; `repoDir` wird im nächsten Task gebraucht und bleibt):

```nix
{
  config,
  lib,
  mkDevSource,
  ...
}: let
  repoDir = "${config.home.homeDirectory}/projects/dotfiles";
```

- [ ] **Step 3: Call-Sites anpassen + `opencode.jsonc` entsperren**

Ersetze den Block

```nix
  xdg.configFile =
    (collectFiles ../../home/config [
      # These paths are generated by dedicated Home Manager modules.
      "alacritty/alacritty.toml"
      "gtk-3.0/settings.ini"
      "user-dirs.dirs"
      "yazi/keymap.toml"
      "yazi/yazi.toml"
      "opencode/opencode.jsonc"
      # nono sandbox profile for AI agents; linked out-of-store below so edits
      # to allow/deny rules take effect without a Home Manager switch.
      "nono/profiles/agent.json"
    ])
    // {
      "opencode/opencode.jsonc".source =
        config.lib.file.mkOutOfStoreSymlink "${repoDir}/home/config/opencode/opencode.jsonc";
      "nono/profiles/agent.json".source =
        config.lib.file.mkOutOfStoreSymlink "${repoDir}/home/config/nono/profiles/agent.json";
    };
  home.file = collectFiles ../../home/files [
    # GTK is generated from the declarative gtk options.
    ".gtkrc-2.0"
  ];
```

durch (opencode.jsonc nicht mehr geskippt/out-of-store; agent.json bleibt geskippt und wird in Task 4 generiert):

```nix
  xdg.configFile = collectFiles "home/config" ../../home/config [
    # These paths are generated by dedicated Home Manager modules.
    "alacritty/alacritty.toml"
    "gtk-3.0/settings.ini"
    "user-dirs.dirs"
    "yazi/keymap.toml"
    "yazi/yazi.toml"
    # nono sandbox profile for AI agents: nix-generiert (Task 4), nicht kopiert.
    "nono/profiles/agent.json"
  ];
  home.file = collectFiles "home/files" ../../home/files [
    # GTK is generated from the declarative gtk options.
    ".gtkrc-2.0"
  ];
```

- [ ] **Step 4: Build verifizieren**

Run: `nixos-rebuild build --flake .#gurke 2>&1 | tail -5`
Expected: Build erfolgreich.

- [ ] **Step 5: `opencode.jsonc` ist jetzt Store-Default**

Run:
```bash
nix eval --impure --raw --expr '
  let hm = (builtins.getFlake (toString ./.)).nixosConfigurations.gurke.config.home-manager.users.felix;
  in builtins.toString hm.xdg.configFile."opencode/opencode.jsonc".source'
```
Expected: ein `/nix/store/...`-Pfad (nicht mehr `/home/felix/projects/dotfiles/...`).

- [ ] **Step 6: Dev-Link für `opencode.jsonc` prüfen**

Run:
```bash
nix eval --impure --raw --expr '
  let sys = (builtins.getFlake (toString ./.)).nixosConfigurations.gurke.extendModules {
        modules = [{ home-manager.users.felix.my.devLinks = [ "home/config/opencode/opencode.jsonc" ]; }];
      };
  in builtins.toString sys.config.home-manager.users.felix.xdg.configFile."opencode/opencode.jsonc".source'
```
Expected: `/home/felix/projects/dotfiles/home/config/opencode/opencode.jsonc`.

- [ ] **Step 7: Commit**

```bash
git add modules/home-manager/dotfiles.nix
git commit -m "refactor(home): route collectFiles through mkDevSource; opencode.jsonc store-default"
```

---

### Task 4: `agent.json` nix-generieren mit abgeleiteten Read-Pfaden

**Files:**
- Modify: `modules/home-manager/dotfiles.nix`

**Hinweis (nono-Semantik, via `nono profile guide` verifiziert):**
`filesystem.read` = read-only **Verzeichnisse**, `filesystem.read_file` =
read-only **Einzeldateien**. Array-Felder werden bei Profil-Vererbung angehängt
und dedupliziert. Typ pro `devLinks`-Eintrag wird über `builtins.readDir` des
Eltern-Verzeichnisses im nix-Source-Tree bestimmt (rein, kopiert nichts in den
Store).

- [ ] **Step 1: `pkgs` + Repo-Source-Root in die Argumente/let-Bindung**

In `modules/home-manager/dotfiles.nix` den Header

```nix
{
  config,
  lib,
  mkDevSource,
  ...
}: let
  repoDir = "${config.home.homeDirectory}/projects/dotfiles";
```

ersetzen durch:

```nix
{
  config,
  lib,
  pkgs,
  mkDevSource,
  ...
}: let
  repoDir = "${config.home.homeDirectory}/projects/dotfiles";
  # nix-Pfad zur Repo-Wurzel (dieses Modul liegt unter modules/home-manager/),
  # um Datei/Verzeichnis-Typ von devLinks-Einträgen rein zu bestimmen.
  repoRoot = ../..;

  # nono agent.json wird generiert: Basis aus der gepflegten Repo-Datei, plus
  # aus my.devLinks abgeleitete read-only Leserechte auf die out-of-store Pfade.
  # Siehe docs/superpowers/specs/2026-06-16-dev-links-design.md.
  agentBase = builtins.fromJSON (builtins.readFile ../../home/config/nono/profiles/agent.json);

  # "directory" -> read (Verzeichnis), sonst read_file (Einzeldatei).
  devLinkIsDir = relPath:
    (builtins.readDir (dirOf (repoRoot + "/${relPath}"))).${baseNameOf relPath} == "directory";
  devLinkAbs = relPath: "${repoDir}/${relPath}";

  devLinkDirs = map devLinkAbs (builtins.filter devLinkIsDir config.my.devLinks);
  devLinkFiles = map devLinkAbs (builtins.filter (p: !devLinkIsDir p) config.my.devLinks);

  agentProfile =
    agentBase
    // {
      filesystem =
        agentBase.filesystem
        // {
          read = agentBase.filesystem.read ++ devLinkDirs;
          read_file = agentBase.filesystem.read_file ++ devLinkFiles;
        };
    };

  agentJson = (pkgs.formats.json {}).generate "nono-agent-profile.json" agentProfile;
```

- [ ] **Step 2: Generierte `agent.json` verlinken**

In `modules/home-manager/dotfiles.nix`, in der `xdg.configFile`-Zuweisung,
ersetze

```nix
  xdg.configFile = collectFiles "home/config" ../../home/config [
```

durch (mergt den generierten Profil-Link hinzu):

```nix
  xdg.configFile =
    (collectFiles "home/config" ../../home/config [
```

und schließe den `collectFiles`-Aufruf direkt nach der Skip-Liste mit dem Merge ab. Ersetze

```nix
    # nono sandbox profile for AI agents: nix-generiert (Task 4), nicht kopiert.
    "nono/profiles/agent.json"
  ];
  home.file = collectFiles "home/files" ../../home/files [
```

durch:

```nix
    # nono sandbox profile for AI agents: nix-generiert (unten), nicht kopiert.
    "nono/profiles/agent.json"
  ])
  // {
    "nono/profiles/agent.json".source = agentJson;
  };
  home.file = collectFiles "home/files" ../../home/files [
```

- [ ] **Step 3: Build verifizieren**

Run: `nixos-rebuild build --flake .#gurke 2>&1 | tail -5`
Expected: Build erfolgreich.

- [ ] **Step 4: Default-`agent.json` enthält keine devLinks-Read-Pfade**

Run:
```bash
nix eval --impure --json --expr '
  let
    hm = (builtins.getFlake (toString ./.)).nixosConfigurations.gurke.config.home-manager.users.felix;
    p = hm.xdg.configFile."nono/profiles/agent.json".source;
  in (builtins.fromJSON (builtins.readFile p)).filesystem.read'
```
Expected: die bestehende read-Liste, **ohne** `/home/felix/projects/dotfiles/...`-Einträge.

- [ ] **Step 5: Mit devLinks abgeleitete Read-Pfade prüfen (Dir + File)**

Run:
```bash
nix eval --impure --json --expr '
  let
    sys = (builtins.getFlake (toString ./.)).nixosConfigurations.gurke.extendModules {
      modules = [{ home-manager.users.felix.my.devLinks = [
        "modules/home-manager/profiles/ai-agents/pi-extensions/context-prune.ts"
        "modules/home-manager/profiles/ai-agents/pi-extensions/actor-swarm"
      ]; }];
    };
    p = sys.config.home-manager.users.felix.xdg.configFile."nono/profiles/agent.json".source;
    fs = (builtins.fromJSON (builtins.readFile p)).filesystem;
  in { read = fs.read; read_file = fs.read_file; }'
```
Expected: `read` enthält `.../pi-extensions/actor-swarm` (Verzeichnis), `read_file` enthält `.../pi-extensions/context-prune.ts` (Datei).

- [ ] **Step 6: Commit**

```bash
git add modules/home-manager/dotfiles.nix
git commit -m "feat(nono): generate agent.json with read paths derived from my.devLinks"
```

---

### Task 5: Reproduzierbarkeits-Regression prüfen (nvd-Diff, Default)

**Files:** keine (Verifikation).

**Ziel:** Mit leerer `my.devLinks` darf sich die erzeugte Konfiguration gegenüber
vor dem Refactor nur dort unterscheiden, wo beabsichtigt (opencode.jsonc und
agent.json sind nun Store statt out-of-store; der agent.json-*Inhalt* bleibt
identisch).

- [ ] **Step 1: Build des aktuellen Standes**

Run: `nixos-rebuild build --flake .#gurke && readlink -f result`
Expected: ein `/nix/store/...-nixos-system-...`-Pfad. Notiere ihn.

- [ ] **Step 2: agent.json-Inhalt unverändert vs. Repo-Quelle (Default)**

Run:
```bash
nix eval --impure --json --expr '
  let
    hm = (builtins.getFlake (toString ./.)).nixosConfigurations.gurke.config.home-manager.users.felix;
    gen = builtins.fromJSON (builtins.readFile hm.xdg.configFile."nono/profiles/agent.json".source);
    src = builtins.fromJSON (builtins.readFile ./home/config/nono/profiles/agent.json);
  in gen == src'
```
Expected: `true` (generiertes Profil == Repo-Quelle, weil devLinks leer).

- [ ] **Step 3: Keine unbeabsichtigten out-of-store-Links übrig**

Die einzige verbleibende `mkOutOfStoreSymlink`-Nutzung außerhalb von `dev-links.nix` soll noctalias Runtime-Dir sein. Verifizieren mit:
```bash
grep -rn "mkOutOfStoreSymlink" modules/ | grep -v "\.md:" | grep -v "dev-links.nix"
```
Expected: nur Treffer in `modules/home-manager/desktops/noctalia-niri.nix`.

- [ ] **Step 4: Commit (nur falls dieser Task Doku/Notizen ergänzt — sonst überspringen)**

Kein Code-Change in diesem Task; nichts zu committen, wenn alle Checks grün sind.

---

### Task 6: End-to-end (manuell, User-Switch)

**Files:** keine (manuelle Funktionsprüfung).

> Dieser Task aktiviert das System — **der User führt `nrs` selbst aus**. Der Agent
> bereitet nur vor und prüft hinterher die Symlinks.

- [ ] **Step 1: Default-Switch (leere devLinks)**

User: `nrs` (gleiche Specialisation wie aktiv).
Danach prüfen:
```bash
ls -l ~/.pi/agent/extensions/ ~/.config/opencode/opencode.jsonc ~/.config/nono/profiles/agent.json
```
Expected: alle zeigen in `/nix/store/...` (außer ggf. noctalia-Runtime-Dir, nicht hier).

- [ ] **Step 2: Dev-Link aktivieren**

In `hosts/gurke/home.nix` (oder passender HM-Stelle) setzen:
```nix
  my.devLinks = [ "modules/home-manager/profiles/ai-agents/pi-extensions/context-prune.ts" ];
```
User: `nrs`.
Danach:
```bash
readlink ~/.pi/agent/extensions/context-prune.ts
nono profile show agent | grep -A20 read
```
Expected: Symlink zeigt auf `~/projects/dotfiles/modules/.../pi-extensions/context-prune.ts`; `agent` profile listet `read_file` mit diesem Pfad.

- [ ] **Step 3: Live-Edit ohne Rebuild**

Eine harmlose Änderung an `context-prune.ts` im Repo machen, in pi `/reload` ausführen.
Expected: Änderung greift ohne `nrs`.

- [ ] **Step 4: Wieder entfernen**

`my.devLinks = [];` (bzw. Zeile entfernen) → User `nrs`.
Expected: Symlink zeigt wieder in den Store; `read_file`-Eintrag in `nono profile show agent` weg.

- [ ] **Step 5: Commit der temporären devLinks-Nutzung NICHT einchecken**

`my.devLinks`-Einträge sind ephemer — nicht committen. Nur die Mechanik (Tasks 1–4) ist committed.

---

## Self-Review

- **Spec coverage:** Option `my.devLinks` (T1) ✓; gemeinsamer Helper (T1, genutzt in T2/T3) ✓; opencode.jsonc Store-Default + dev-linkbar (T3) ✓; agent.json nix-generiert mit abgeleiteten read/read_file (T4) ✓; noctalia bleibt unangetastet (T5 Step 3) ✓; Reproduzierbarkeit/Default-Identität (T5) ✓; Workflow end-to-end (T6) ✓.
- **Placeholder scan:** keine TBD/TODO; alle Code-Blöcke vollständig.
- **Type/Name-Konsistenz:** `mkDevSource relPath storePath` einheitlich definiert (T1) und aufgerufen (T2: `mkDevSource "${relRoot}/${name}" (dir + "/${name}")`; T3: `mkDevSource "${repoRelRoot}/${relPath}" path`). `collectFiles repoRelRoot root skipped` Signatur durchgängig (beide Call-Sites mit Präfix). `agentProfile`/`agentJson` konsistent referenziert.
